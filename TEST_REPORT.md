# FloVo v1.0.0 テスト結果

実施日: 2026.09.23

対象: v1.0.0 / Build 481

レビュー開始版: v1.0.0 / Build 477

## 結果概要

`TEST_PLAN.md` の91項目を、実操作、ソース検査、テスト用Excel、公開候補版のブラウザ操作で確認した。

| 区分 | 合格 | 要実機・部分確認 | 未解決の失敗 |
|---|---:|---:|---:|
| A 起動・ホーム・PWA | 7 | 2 | 0 |
| B Excel入出力 | 8 | 4 | 0 |
| C 絞り込み・統計 | 7 | 3 | 0 |
| D 練習カード | 7 | 4 | 0 |
| E 補足・文編集 | 9 | 1 | 0 |
| F データ編集 | 7 | 6 | 0 |
| G 音声・自動再生 | 3 | 5 | 0 |
| H 非機能・コード品質 | 8 | 2 | 0 |
| **合計** | **56** | **35** | **0** |

「要実機・部分確認」は不合格ではない。クラウドChromeでは音声品質、実音声の中断、iPhone Safari固有のタッチ・共有・オフライン復帰を完全には再現できないため、コード検査または部分操作までとした項目である。

## 発見・修正した問題

### P1: 編集モーダル表示中に背面画面が操作される

- 再現: 練習カードからデータ編集を開き、Escapeを押す。
- 修正前: 編集画面ではなく背面の練習画面が閉じ、編集モーダルだけがホーム上に残った。矢印キーも背面カードへ届いた。
- 修正: 表示中モーダルを最優先で処理し、モーダル中は背面のキーボード操作を遮断した。
- 回帰: 編集中の右矢印で `1 / 5` のまま変化しないこと、1回目のEscapeでキャンセル確認、2回目で編集画面へ戻ることを確認した。

### P2: Service Workerが他用途のキャッシュも削除し得る

- 修正前: 同一オリジンのキャッシュを、現在のFloVoキャッシュ以外すべて削除していた。
- 修正: `flovo-` で始まる旧キャッシュだけを削除する条件へ限定した。

### P2: モーダルの初期フォーカスと復帰先が不統一

- 修正: データ編集、補足、結果編集、練習設定、学習データリセット等で、表示時にモーダル内へフォーカスし、閉じた後は起点ボタンへ戻すようにした。

### P3: Excel列名の説明が旧仕様

- 修正前: READMEの最終列が旧名の「？回数」だった。
- 修正: 現行仕様の「△回数」へ更新した。旧形式の読込互換性は維持している。

### P2: スライドイン中のEscapeが無視される

- 再現: 日本語・英語・補足の編集画面を開いた直後にEscapeを押す。
- 修正: 遷移中の閉じる要求を保留し、スライドイン完了直後に必ず実行するようにした。

## 主な実施結果

- 正常な19列・6行のテストExcelを読込み、4単語、3出題対象単語、5例文として集計されることを確認。
- 一覧順、カード表示、日本語・英語・補足、発音記号、S/W、理解度、回答回数を確認。
- 理解度と回答回数を変更し、再読込後も残ること、テスト後に元の値へ戻せることを確認。
- 前方一致 `acc` でacceptの2例文だけになること、文字条件リセットで全5例文へ戻ることを確認。
- 全条件解除時に適用が無効になり、キャンセルで27選択へ戻ることを確認。
- 補足の内側タップでは閉じず、背景・×・Escapeで閉じることを確認。
- 日本語・英語・補足の編集タイトル、保存・キャンセル、英文表示状態と補足表示状態の維持を確認。
- データ編集のキャンセル確認、例文2件の表示、背面操作遮断、フォーカス復帰を確認。
- 単語US/UK、日本語、英語の音声ボタンが有効で、呼出し時に例外が出ないことを確認。実際の声質と端末スピーカー出力は実機確認対象。
- JavaScript構文、HTML ID 191件の重複、JavaScriptからのID参照179件、Service Worker資産9件、秘密情報、ビルド番号とキャッシュ番号の一致を検査し合格。

## 回帰テスト

Service Workerを変更したため、当初予定の2回に1回追加し、合計3回とした。

1. 修正直後: 構文、参照、資産、秘密情報、差分検査と主要データ・画面導線を確認。
2. 公開候補 Build 478: 公開URLでバージョン、編集モーダル、Escape、背面キー遮断、保存状態を確認。
3. 最終 Build 481: 補足フォーカスと遷移中Escape修正を含む公開版で主要導線を再確認。

## 継続課題

- `app.js` は約2,800行で共有状態とイベント処理が集中している。機能別モジュール化が必要。
- CSSは後段の上書きが複数あり、見た目変更時の影響範囲が読みづらい。コンポーネント単位への整理が必要。
- `alert` / `confirm` 依存により自動E2Eが停止しやすい。アプリ内ダイアログへ統一するとテストしやすい。
- データ保存、採番、検証、フィルタ条件を純粋関数へ分離し、自動単体テストを追加する余地がある。
- iPhone Safariで、左右フリック、PWAオフライン復帰、共有シート経由のExcel保存、実音声の停止・再開を最終確認する。
